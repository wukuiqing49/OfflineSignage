# Copy to the ignored keystore.properties and fill locally, or use CI environment variables.
# Keep the existing application certificate when updating an already installed/published app.
storeFile=
storePassword=
keyAlias=
keyPassword=
playLicensePublicKey=
